Note: For this to work fully in production, you will need to:

Ensure your backend endpoint /api/v1/users/me accepts pushToken.
Configure your app credentials in Expo (EAS).
Test on a physical device (Simulators cannot receive push notifications).